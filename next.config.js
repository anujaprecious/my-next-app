/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: [
    "mytest.com",
    "mytest.com",
    "mytest.com",
    "mytest.com",
  ]
}

module.exports = nextConfig
